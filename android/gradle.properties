org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Workaround for Kotlin daemon incremental cache issues on Windows when
# the Flutter pub cache and project live on different drive roots.
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false
