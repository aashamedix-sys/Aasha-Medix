import 'package:flutter/material.dart';

class BookNurseVisitScreen extends StatelessWidget {
  const BookNurseVisitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Book Nursing Visit')),
      body: const Center(
        child: Text('Placeholder to collect address and slot'),
      ),
    );
  }
}
